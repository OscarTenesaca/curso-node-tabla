#Notas:

Este es mi primer programa en node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -n, --num      el numero de la tabla de multiplicacion     [number] [required]
  -l, --listar   muestra la tabla de multiplicacion en consola
                                                         [boolean] [default: 10]
  -h, --hasta    muestra el limite hasta donde multiplicar
                                                        [number] [default: true]
```